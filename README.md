# Address Geocoder

Project to build a useful set of information from an address.

## Setup

* create a .env file, and add a key for opencagegeocoding, with the name `OPENCAGE_GEOCODING_API_KEY`
* npm install
* npm run dev
